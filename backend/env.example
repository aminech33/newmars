# Configuration Backend Adaptatif

# OpenAI API Key (obligatoire)
OPENAI_API_KEY=your_openai_api_key_here

# Serveur (optionnel)
HOST=0.0.0.0
PORT=8000
DEBUG=True

# ============================================
# Withings API Integration (optionnel)
# ============================================
# Pour connecter ta balance Withings et synchroniser automatiquement tes pesées
# 
# DÉVELOPPEMENT (avec ngrok) :
# 1. Crée un compte développeur : https://developer.withings.com/
# 2. Crée une application pour obtenir CLIENT_ID et CLIENT_SECRET
# 3. Lance ngrok : ngrok http 8000
# 4. Note l'URL HTTPS (ex: https://abc123.ngrok-free.app)
# 5. Configure cette URL + /api/withings/callback dans Withings Developer
# 6. Mets à jour WITHINGS_REDIRECT_URI ci-dessous avec ton URL ngrok
#
# PRODUCTION :
# Remplace l'URL ngrok par ton vrai domaine HTTPS

WITHINGS_CLIENT_ID=your_withings_client_id
WITHINGS_CLIENT_SECRET=your_withings_client_secret
WITHINGS_REDIRECT_URI=https://TON-URL-NGROK.ngrok-free.app/api/withings/callback
WITHINGS_WEBHOOK_SECRET=your_webhook_secret_optional
